import {Metric,Empty} from '../ui.jsx'
export default function Diagnostics({data}){const logs=[...(data?.logs||[])].reverse();return <div className="two"><div className="card"><div className="head"><h3>System Diagnostics</h3><span className="tag">KITT</span></div><div className="metrics"><Metric label="Engine" value={data?.running?'RUNNING':'STANDBY'}/><Metric label="Mode" value={data?.paperTrade?'PAPER':'LIVE'}/><Metric label="Strategy" value={data?.strategyName||'—'}/><Metric label="Feed requests" value={data?.feedStats?.requests??data?.feedStats?.ticks??0}/><Metric label="Feed error" value={data?.feedStats?.lastError||'OK'}/></div></div><div className="card"><div className="head"><h3>Live logs</h3></div><div className="logs">{logs.length?logs.map((l,i)=><div className="log" key={i}><time>{l.time}</time><span>{l.msg}</span></div>):<Empty>Nincs log.</Empty>}</div></div></div>}
