import {money,Metric} from '../ui.jsx'
export default function Market({data}){
 const prices=data?.lastPrices||{},stats=data?.strategyStats||{}
 return <div className="stack"><div className="hud-card intro-panel"><span className="eyebrow">LIVE MARKET TELEMETRY</span><h2>Market Radar</h2><p>Coinbase árfolyamok és az aktív stratégia valós idejű telemetriája.</p></div><div className="market-grid">{Object.entries(prices).map(([pair,price],i)=>{const s=stats[pair]||{};return <div className="hud-card market-card" key={pair}><div className="panel-head"><span>◈ {pair}</span><span className="live-tag">LIVE</span></div><div className="market-price">{money(price,price<10?4:2)}</div><div className={i%4===3?'spark market-spark down':'spark market-spark'}><i/><i/><i/><i/><i/><i/><i/></div><div className="metrics"><Metric label="RSI" value={s.rsi==null?'—':Number(s.rsi).toFixed(1)}/><Metric label="Change" value={s.change==null?'—':`${(s.change*100).toFixed(3)}%`}/><Metric label="Threshold" value={s.dynamicThreshold==null?'—':`${(s.dynamicThreshold*100).toFixed(3)}%`}/></div></div>})}</div></div>
}