import {TradeList,Card,money} from '../ui.jsx'
export default function Trades({data,lab}){const trades=[...(data?.trades||[])].reverse(),a=lab?.tradeAnalysis||{};return <div className="stack"><div className="stats"><Card label="LEZÁRT" value={a.closedTrades||0} sub={`${a.wins||0} nyerő / ${a.losses||0} vesztes`}/><Card label="WIN RATE" value={a.winRate==null?'—':`${(a.winRate*100).toFixed(1)}%`} sub={`Max loss streak ${a.maxLossStreak||0}`}/><Card label="REALIZED P/L" value={money(a.realizedPnl)} sub="Lezárt pozíciók" tone={Number(a.realizedPnl||0)>=0?'positive':'negative'}/></div><div className="card"><div className="head"><div><span className="eyebrow">ANALYZED TRADES</span><h3>Kielemzett tradek</h3></div><span className="tag">{trades.length}</span></div><TradeList trades={trades}/></div></div>}
