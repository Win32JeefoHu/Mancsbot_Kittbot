import {TradeList,money} from '../ui.jsx'
export default function Trades({data,lab}){const trades=[...(data?.trades||[])].reverse(),a=lab?.tradeAnalysis||{};return <div className="stack"><div className="hud-stats"><div className="hud-card stat"><small>LEZÁRT TRADE</small><strong>{a.closedTrades||0}</strong><em>{a.wins||0} nyerő / {a.losses||0} vesztes</em></div><div className="hud-card stat"><small>WIN RATE</small><strong>{a.winRate==null?'—':`${(a.winRate*100).toFixed(1)}%`}</strong><em>Loss streak {a.maxLossStreak||0}</em></div><div className="hud-card stat"><small>REALIZED P/L</small><strong className={Number(a.realizedPnl||0)>=0?'positive':'negative'}>{money(a.realizedPnl)}</strong><em>Lezárt pozíciók</em></div></div><div className="hud-card"><div className="panel-head"><span>⇄ ANALYZED TRADES</span><span className="live-tag">{trades.length} RECORD</span></div><TradeList trades={trades}/></div></div>}
